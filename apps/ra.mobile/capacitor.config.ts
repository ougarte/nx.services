import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
	appId: 'io.ionic.starter',
	appName: 'ra.mobile',
	webDir: '../../dist/apps/ra.mobile',
	bundledWebRuntime: false,
};

export default config;
